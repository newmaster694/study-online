server:
    port: 8081

spring:
    application:
        name: content-service
    datasource:
        url: "jdbc:mysql://localhost:3306/so_content?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false&zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Shanghai"
        driver-class-name: com.mysql.jdbc.Driver
        username: "root"
        password: "000016"
    cloud:
        nacos:
            server-addr: 127.0.0.1:8848

mybatis-plus:
    configuration:
        map-underscore-to-camel-case: true # mybatis驼峰转换配置

logging:
    level:
        study.online.content:
            mapper: debug
            service: info
            controller: info