spring:
    cloud:
        nacos:
            server-addr: 127.0.0.1:8848
            config:
                file-extension: yml
                refresh-enabled: true
                shared-configs:
                    -   data-id: 'database-config.yml'

    profiles:
        active: dev
