spring:
    application:
        name: "system-service"
    cloud:
        nacos:
            server-addr: 127.0.0.1:8848
            config:
                file-extension: "yaml"
                shared-configs:
                - data-id: "database-config.yml"

study:
    online:
        mysql:
            database: "so_system"
            host: "127.0.0.1"
            username: "root"
            password: "000016"
        redis:
            host: "127.0.0.1"