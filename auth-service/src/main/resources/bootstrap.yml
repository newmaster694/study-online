spring:
    application:
        name: "auth_service"
    cloud:
        nacos:
            server-addr: 127.0.0.1:8848
            config:
                file-extension: "yml"
                shared-configs:
                    -   data-id: "database-config.yml"
                    -   data-id: "redis-config.yml"

study:
    online:
        mysql:
            database: "so_user"
            host: "127.0.0.1"
            username: "root"
            password: "000016"
        redis:
            host: "127.0.0.1"